{{- if .Values.kialiOperator.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kiali-operator
  namespace: {{ .Values.kialiOperator.namespace }}
  labels:
    app: kiali-operator
    release: {{ .Release.Name }}
spec:
  replicas: {{ .Values.kialiOperator.replicaCount }}
  selector:
    matchLabels:
      app: kiali-operator
  template:
    metadata:
      name: kiali-operator
      labels:
        app: kiali-operator
        release: {{ .Release.Name }}
      annotations:
        sidecar.istio.io/inject: "false"
        scheduler.alpha.kubernetes.io/critical-pod: ""
        {{- if .Values.kialiOperator.podAnnotations }}
{{ toYaml .Values.kialiOperator.podAnnotations | indent 8 }}
        {{- end }}
    spec:
      serviceAccountName: kiali-operator-service-account
{{- if .Values.global.priorityClassName }}
      priorityClassName: "{{ .Values.global.priorityClassName }}"
{{- end }}
{{- if .Values.kialiOperator.tolerations }}
      tolerations:
{{ toYaml .Values.kialiOperator.tolerations | indent 8 }}
{{- end }}
      containers:
      - name: ansible
        command:
        - /usr/local/bin/ao-logs
        - /tmp/ansible-operator/runner
        - stdout
        image: "{{ .Values.kialiOperator.hub }}/{{ .Values.kialiOperator.image }}:{{ .Values.kialiOperator.tag }}"
        imagePullPolicy: {{ .Values.global.imagePullPolicy | default "Always" }}
        volumeMounts:
        - mountPath: /tmp/ansible-operator/runner
          name: runner
          readOnly: true
        resources:
{{- if .Values.kialiOperator.resources }}
{{ toYaml .Values.kialiOperator.resources | indent 10 }}
{{- else }}
{{ toYaml .Values.global.defaultResources | indent 10 }}
{{- end }}
      - name: operator
        image: "{{ .Values.kialiOperator.hub }}/{{ .Values.kialiOperator.image }}:{{ .Values.kialiOperator.tag }}"
        imagePullPolicy: {{ .Values.global.imagePullPolicy | default "Always" }}
        volumeMounts:
        - mountPath: /tmp/ansible-operator/runner
          name: runner
        env:
        - name: WATCH_NAMESPACE
          value: ""
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: OPERATOR_NAME
          value: "kiali-operator"
        resources:
{{- if .Values.kialiOperator.resources }}
{{ toYaml .Values.kialiOperator.resources | indent 10 }}
{{- else }}
{{ toYaml .Values.global.defaultResources | indent 10 }}
{{- end }}
      volumes:
      - name: runner
        emptyDir: {}
      affinity:
      {{- include "nodeaffinity" . | indent 6 }}
      {{- include "podAntiAffinity" . | indent 6 }}
{{- end }}
