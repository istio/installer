apiVersion: apps/v1
kind: Deployment
metadata:
  name: istio-ingressgateway
spec:
  template:
    spec:
      containers:
        - name: istio-proxy
          resources:
            limits:
              memory: 50Mi
            requests:
              memory: 50Mi

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: istio-pilot
spec:
  template:
    spec:
      containers:
        - name: discovery
          resources:
            limits:
              memory: 50Mi
            requests:
              memory: 50Mi
